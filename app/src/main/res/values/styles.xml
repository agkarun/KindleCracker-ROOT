<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="startStyle" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/startButtonDark</item>
        <item name="colorButtonNormal">@color/startButton</item>
    </style>

    <style name="stopStyle" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/stopButtonDark</item>
        <item name="colorButtonNormal">@color/stopButton</item>
    </style>

</resources>
